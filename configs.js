var user = 'gabo'
var host = 'localhost'
var database = 'testDb'
var password = ''
var dbport = 5432
var serverport = 3000
var host_url = "0.0.0.0"

module.exports = {
    user,
    host,
    database,
    password,
    dbport,
    serverport,
    host_url
}
